# تقرير تنفيذ توجيه المنارة — المستوى A + وثائق B

**التاريخ:** 2026-07-22 · **الفرع:** `feat/manara-a-level` (غير مدموج — بانتظار قرار المالك) · **الأساس:** `main @ e413a4d`

---

## 1) جدول حالة بنود المستوى A

| البند | الوصف | الحالة | نتيجة الاختبار |
|---|---|---|---|
| **A1** | بوابة موافقة المريض قبل التسجيل | ✅ منجز (باك + واجهة) | `test_visit_flow` يؤكد MDF-4230 قبل الموافقة ثم النجاح بعدها · تحقّق حيّ في المتصفح: شاشة ثنائية اللغة، زر بدء التسجيل معطّل حتى الإقرار |
| **A2** | فصل الاعتماد لبوابتين ①② | ✅ منجز | `test_note_approve_gate_precedes_code_approve`: ② مرفوضة قبل ① (MDF-4231)، ① تجمّد النص (MDF-4226)، لكل بوابة بصمة SHA-256 وحدث تدقيق · تحقّق حيّ |
| **A3** | تصدير PDF بترويسة ثنائية + نسخ للـEMR | ✅ منجز | `test_export_blocked_before_gate_two` (MDF-4232) · `test_export_pdf_after_gate_two` (PDF فعلي %PDF 19KB) · `test_export_text_after_gate_two` · تحقّق حيّ للزرّين |
| **A4** | حذف الصوت المجدول (سياسة الاحتفاظ) | ✅ منجز | `test_scheduled_purge_deletes_expired_audio`: يحذف، يختم `deleted_at`، حدث تدقيق لكل حذف، idempotent |
| **A5** | ثقة الأكواد + حجب دون العتبة | ✅ منجز | `test_low_confidence_code_is_withheld_until_doctor_input` · `test_structured_plan_items_keep_mandated_code_systems` (SFDA/GMDN لا تُقسر) · تحقّق حيّ: بطاقة «يتطلب إدخال الطبيب» بخانة كود فارغة |
| **A6** | بيئة demo كاملة | ✅ منجز | قاعدة `medify_demo` مُشيت وبُذرت · مسار كامل تحقّق حياً في المتصفح: موافقة → تسجيل → ملخص → أكواد بثقة → ① → ② → PDF |

**إجمالي الاختبارات:** 95 pytest ناجحة (منها 9 جديدة للمستوى A) · `tsc --noEmit` نظيف · `next build` ناجح.

### تغييرات العقد المرافقة
- **رموز MDF:** صارت 27 (+MDF-4230 موافقة · +4231 قبل ① · +4232 تصدير قبل ②).
- **أحداث التحليلات:** صارت 14 (+`visit.note_approved` · +`note.exported`) — بلا محتوى سريري.
- **هجرة 0004:** `visit_consents` + `note_approvals` (إلحاقيان) · أعمدة الترميز المهيكل والثقة على `guidance_items` + CHECK مبرر الجهاز · trigger موافقة قبل التسجيل · تجميد النص من البوابة ①. idempotent.
- **مطالبة P3 @1.1:** بنود خطة مهيكلة بأنظمتها المقررة + ثقة + provenance.

---

## 2) وثائق المستوى B (٥ ملفات HTML A4 RTL جاهزة للطباعة)

المسار: `medify/docs/manara/`

| # | الملف | الغرض |
|---|---|---|
| B1 | `B1-integration-discovery.html` | استبيان استكشاف التكامل — يُملأ بالقلم في الجلسة (**أهم مخرج**) |
| B2 | `B2-integration-spec-v1.html` | مواصفة التكامل v1 + قالب Field Mapping فارغ |
| B3 | `B3-rollout-plan.html` | خطة نشر مرحلية sandbox→UAT→production بمعايير خروج ومخاطر |
| B4 | `B4-data-protection.html` | ملخص حماية البيانات صفحة واحدة (مُدقَّق كاملاً) |
| B5 | `B5-session-agenda.html` | أجندة الجلسة (ساعة) + ما نطلب حضوره + الخطوات التالية |

**للطباعة:** افتح كل ملف في المتصفح ثم اطبع (Ctrl+P، A4). الخطوط والتنسيق مضمَّنة ذاتياً (بلا موارد خارجية).

---

## 3) العوائق والحلول المؤقتة

| العائق | الأثر | الحل المؤقت المطبَّق |
|---|---|---|
| اسم نظام المستشفى وقدراته غير معروفة قبل الجلسة | لا يمكن بناء الموصّل العميق الأول بعد | **B1** يستخرجها في الجلسة؛ الطبقة معيارية (FHIR R4 + مهايئ HL7v2) تنتظر ملء الـMapping |
| التكامل الحي غير جاهز يوم التوقيع | لا رفع آلي فوري لنظام المستشفى | **وضع الجسر (A3):** PDF بترويسة + نسخ نظيف للـEMR متاحان فور البوابة ② — المنتج قابل للاستخدام من اليوم الأول |
| مكتبات PDF (reportlab/arabic-reshaper/python-bidi/fonttools) أُضيفت لـ`pyproject.toml` | تتطلب `pip install` عند النشر | مثبّتة محلياً ومُوثّقة في الاعتماديات؛ خطوط الهوية تُحوَّل woff2→TTF في الذاكرة بلا ملفات مكررة |
| محرك STT/LLM على mock في الـdemo | العرض ببيانات اصطناعية | مقصود — «ممنوع أي بيانات حقيقية في العرض» (A6). المحرك يُبدَّل بمتغير بيئة في الإنتاج |

---

## 4) ما يلزم المالك لاحقاً (خارج المستوى A)
- مفتاح Anthropic حقيقي لتفعيل `LLM_ENGINE=claude` في العرض إن أُريد نص حي.
- بيانات اعتماد sandbox من المستشفى (تُطلب في B1) لبدء الموصّل العميق.
- قرار الدمج/الرفع لفرع `feat/manara-a-level` (لم يُرفع بعد).
