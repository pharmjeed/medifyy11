# سجل القرارات — Medify v0.1.0

كل غموض حُسم وفق ترتيب مصادر الحقيقة (قرارات المالك 2026-07-14 → DOC-10 v1.2 → النموذج التفاعلي → بقية الوثائق). القرارات بترتيب زمني.

| # | القرار | السبب / المرجع |
|---|---|---|
| D-01 | وجهة الرفع GitHub هي `pharmjeed/medifyy11` (الريبو الذي وفّره المالك في أمر الجلسة) بدل إنشاء `pharmjeed/medify` المذكور في DOC-07 §٣ | أمر المالك المباشر في الجلسة يتقدم على الوثائق؛ الريبو موجود وفارغ. ملاحظة: الريبو **عام** بينما البرومت طلب خاصاً — تغيير الرؤية صلاحية مالك لا وكيل؛ لا أسرار في الكود فلا خطر. يوصى بجعله خاصاً من إعدادات GitHub |
| D-02 | Docker غير مثبت على جهاز التطوير (Windows) → الاختبارات المحلية تعمل ضد عنقود PostgreSQL 16.13 محلي مستقل (initdb خاص بالجلسة)؛ ملفات compose تُبنى كاملة وتُختبر على الخادم | «لا تتوقف أبداً» — البديل يحقق نفس الضمانات (RLS/triggers حقيقية على PG16) |
| D-03 | `ANTHROPIC_API_KEY` غير متوفر في بيئة الجلسة → `LLM_ENGINE=mock` افتراضياً بمخرجات JSON صالحة سريرياً؛ التبديل لـ`claude` بمتغير بيئة فقط | البرومت §٥ نصاً |
| D-04 | `STT_ENGINE=mock` افتراضياً في dev (مولد نص عربي سريري متدفق)؛ حاوية `faster-whisper` (small, CPU int8) تُفعّل بـ`STT_ENGINE=whisper` | البرومت §٥؛ لا GPU مضمون |
| D-05 | التشفير العمودي تطبيقي (Fernet/AES-128-CBC+HMAC عبر `cryptography`) بمفتاح `COLUMN_ENCRYPTION_KEY` من البيئة، بدل pgcrypto | DOC-04: «pgcrypto أو تشفير تطبيقي» — التطبيقي أبسط نشراً ولا يمرر المفتاح في SQL |
| D-06 | أحداث التحليلات (DOC-14) تُبث إلى مسجّل بنيوي (structured log / stdout) خلف `ANALYTICS_ENGINE=log` — **لا جدول تحليلات** | DOC-04 حصري (لا جدول خارجه)، وDOC-14 يسمح بأداة self-hosted لاحقاً (posthog) |
| D-07 | قفل الحساب بعد 5 محاولات فاشلة (DOC-16) يُنفَّذ في Redis/الذاكرة بنافذة 15 دقيقة + تسجيل في audit_logs — لا عمود جديد في `users` | DOC-04 حصري؛ DOC-16 لا يحدد آلية التخزين |
| D-08 | رفع تعليق المنشأة في webhook السداد: عند سداد فاتورة، إن كانت المنشأة `suspended` ولم يتبقَّ فواتير `overdue` → `active` + تسجيل في audit_logs | DOC-05 «ترفع التعليق إن كان سببه التعثر» — الاستدلال الوحيد المتاح من جداول DOC-04 |
| D-09 | تخزين الصوت: نظام ملفات محلي داخل volume (`RECORDINGS_DIR`) بواجهة storage قابلة للتبديل إلى S3-compatible داخل المملكة في الإنتاج | DOC-07 يتطلب S3-compatible سعودياً — لا مزود متاح في الجلسة؛ الواجهة تعزل القرار |
| D-10 | مزود الدفع: `PAYMENT_ENGINE=mock` (جلسة دفع وهمية + webhook موقّع HMAC) — DOC-09 §٣ يقفل مزوداً واحداً بعد اختبار حقيقي لاحقاً (Moyasar/Tap) | لا مفاتيح مزود في الجلسة؛ «التحقق قبل الالتزام» |
| D-11 | ترقيم UUID v7 عبر مكتبة `uuid6` (دالة uuid7) | DOC-04 يفرض UUID v7؛ المكتبة القياسية بايثون 3.12 لا توفره |
| D-12 | جلسة RLS: `SET LOCAL app.facility_id / app.user_id / app.user_role` داخل معاملة كل طلب بعد التحقق من JWT؛ دور التطبيق `medify_app` غير مالك للجداول فتنطبق عليه السياسات | DOC-04 §٧ |
| D-13 | If-Match/ETag إلزامي على `PATCH /summary-sections/{id}` (تعارض → MDF-4224 / HTTP 412)؛ الـ ETag يُحسب من هاش محتويات الأقسام وإصدار التحديث | DOC-05 §١ «عمليات التعديل على الملخص» |
| D-14 | حالة الزيارة تنتقل `summarized → in_review` تلقائياً فور اكتمال P3 (أو فشله بـ W-224) — المراجعة تبدأ حين تتاح الشاشة للدكتور | آلة الحالات DOC-04 لا تحدد المشغِّل؛ هذا أقرب لسلوك النموذج التفاعلي |
| D-15 | Python محلياً 3.13 (المتوفر) والحاويات 3.12 (DOC-07) — الكود متوافق مع كليهما | «لا تتوقف» |
| D-16 | بريد استعادة كلمة المرور واستقبال الإشعارات الحرجة: `EMAIL_ENGINE=mock` يكتب الرسائل إلى لوج/مجلد `var/outbox` — لا مزود SMTP في الجلسة | البرومت: بديل mock لكل مورد غائب |
| D-17 | `deploy.env` غير موجود في مجلد العمل ولا متغيرات Oracle في البيئة → نُفِّذ كل شيء عدا خطوة SSH؛ `infra/deploy/oracle.sh` جاهز + `deploy.env.example` + أمر تشغيل واحد | البرومت §٩ نصاً |
| D-18 | `seat_events` و`upload_attempts` و`password_reset_tokens` بلا عمود `facility_id` (قوائم أعمدة DOC-04 حرفية) → RLS عليها عبر سياسات JOIN (الاشتراك/المهمة/المستخدم) | تعارض بين مبدأ DOC-04 §١ (كل جدول مستأجري يحمل facility_id) وقوائم الأعمدة الحصرية — القائمة الحصرية تتقدم («لا عمود خارج الوثيقة») والعزل محفوظ بسياسة join |
| D-19 | دوران لقاعدة البيانات: `medify_app` (LOGIN، خاضع لكل سياسات RLS) لكل نقاط الـAPI المصادَقة، ودور المالك للمسارات النظامية الضيقة فقط (login lookup، register، استعادة كلمة المرور، webhook السداد، عامل الرفع، الهجرات، seed) | login وwebhook تسبق وجود سياق `app.facility_id`؛ سطح الهجوم (كل النقاط المصادَقة) يبقى خاضعاً لـRLS بالكامل، واختبارات العزل تمر عبره |
| D-20 | النموذج التفاعلي يستخدم `MDF-4230` لتعليق المنشأة و`MDF-4227` لنص معلق في المراجعة — والتنفيذ يلتزم DOC-13: التعليق = `MDF-4013`، وحجب الاعتماد لوجود `[Not discussed]` يبقى سلوك واجهة (تحذير) دون رمز خادم جديد | ترتيب مصادر الحقيقة: DOC-13 (22 رمزاً حصراً) فوق النموذج التفاعلي |
| D-21 | صفحات النموذج الـ15 تُنفَّذ في Next.js على مسارات: `/login`, `/register`, `/profile`, `/admin`, `/admin/clinics`, `/admin/doctors`, `/admin/subscription`, `/admin/settings`, `/admin/analytics`, `/admin/audit`, `/doctor`, `/doctor/visits`, `/doctor/templates`, `/doctor/visits/new`, `/doctor/visits/[id]/review` | خريطة الدمج DOC-10 §٤ |
| D-22 | إضافة نقطتين خارج DOC-05: `GET /notifications` و`PATCH /notifications/{id}/read` — الحد الأدنى لتشغيل W-003 (مركز الإشعارات) | تعارض وثائقي: DOC-05 حصري لكنه لا يذكر أي نقطة إشعارات، بينما DOC-10 v1.2 (أعلى منه في الترتيب) يفرض W-003 وDOC-12 يفرض `read_at` — القراءة للذات فقط ولا محتوى سريرياً |
| D-23 | `PATCH /me/password` لتغيير كلمة المرور الذاتية (W-005) — امتداد لمسار `/me` الموجود في DOC-05 | DOC-06 §٢: «كلمة مروره فقط عبر /me»؛ W-005 يتطلب المسار |
| D-24 | تعيين القالب الافتراضي (FR-505) متاح للقوالب الشخصية فقط — `is_default` عمود على صف القالب المشترك ولا يمكن جعله لكل دكتور دون جدول جديد (محظور) | قيد DOC-04 الحصري؛ النموذج التفاعلي يسمح بتمييز أي قالب — انحراف موثق للتقرير النهائي |
| D-25 | أخطاء التحقق البنيوي للطلبات (pydantic) تُغلَّف بـ`MDF-5001` مع `details.validation` — لا رمز تحقق عام في الـ22 | DOC-13 حصري ولا يجيز اختراع رمز؛ الواجهة تتحقق قبل الإرسال فلا يظهر للمستخدم عملياً |
| D-26 | في `PAYMENT_ENGINE=mock` يسوّي `POST /invoices/{id}/pay` الفاتورة فوراً (يحاكي redirect+webhook متزامنين) ليعمل W-208 كاملاً من الواجهة؛ نقطة `POST /webhooks/payments` الموقعة تبقى المسار الرسمي للمزود الحقيقي | لا مفاتيح مزود في الجلسة؛ الواجهة لا تستطيع توقيع webhook (السر خادمي) |
| D-27 | وسم `v0.1.0` أُعيد توجيهه بعد دقائق من إنشائه لأن سطر `models/` في `.gitignore` (المقصود أوزان AI) ابتلع حزمة `backend/app/models` من الدفعات الأولى — الوسم النهائي يشير للنسخة السليمة الكاملة | إصلاح خلال الجلسة نفسها وقبل أي استهلاك للوسم |
